# Áudio de locução

Coloque a locução final em `public/locucao-sigess.mp3` e altere a propriedade
`voiceoverFile` da composição principal para `locucao-sigess.mp3` no Remotion Studio.

O vídeo foi temporizado para 80 segundos e segue o roteiro registrado em
`src/story.ts`.
